{
  "name": "facegram",
  "version": "1.0.0",
  "description": "Something like social media",
  "main": "index.js",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "express-es6-template-engine": "^2.2.3",
    "express-session": "^1.17.2",
    "faker": "^5.5.3",
    "passport": "^0.4.1",
    "passport-github": "^1.1.0",
    "pg-promise": "^10.10.2",
    "uuid": "^8.3.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.7",
    "prettier-eslint-cli": "^5.0.1"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon index.js",
    "db:create": "createdb facegram",
    "db:destroy": "dropdb facegram",
    "db:init": "psql -d facegram < ./db/schema.sql",
    "db:seed": "node ./db/seed.js",
    "db:reset": "npm run db:destroy; npm run db:create && npm run db:init && npm run db:seed",
    "pretty": "prettier-eslint --write \"*.js\""
  },
  "author": "",
  "license": "ISC",
  "keywords": []
}
